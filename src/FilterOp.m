classdef FilterOp < int32
    % enumeration used with the import routines
    % Each import routine takes a first argument that describes the behavior
    enumeration
        getDescription (0)
        getWebpage     (2)
        importCatalog  (1)
    end
end