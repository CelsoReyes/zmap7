function activate_result_axes(obj,ax)
    
    % wait for mouse click
    % when click in axes. 
    % - find grid point. 
    % - get events associated with grid point
    % - plot each subplot for these points
    
    % enable shape analysis
    
    
    %% currently, before writing, the Main window would be in this state:
    % f.WindowButtonMotionFcn is @ZmapHGridFunction.interact/update
    % f.WindowButtonDownFcn is @callbacks.cropBasedOnAxis
    % f.
    error('not yet implemented')
end
